#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Main is a special type of function which I term a wrapper
 * function. It serves as the entry point for all C programs and it is executed
 * first.
 * Return: Always 0 (Success)
 */
int main(void)
{
	return (0);
}
